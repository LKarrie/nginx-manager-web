import Card from '@/components/card';

export default function UserDetail() {
  // const { id } = useParams();
  // const user = USERS.find((user) => user.id === id);
  return (
    <Card>
      <p>这是用户 username 的详情页面</p>
    </Card>
  );
}
